import React from 'react';

const Loading = () => {
    return (
        <div>
            <h2>Loading...</h2>
            {/* Add loading animation or message */}
        </div>
    );
}

export default Loading;
